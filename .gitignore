# RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
# Quarto
/.quarto/
**/*.quarto_ipynb
# knitr cache (chapter-level folders e.g. 02DecompTrends_cache/)
*_cache/
# stray support folders left in root by single-file "Render" (not "Render Book")
/*_files/
# intermediate files
*.log
*.aux
*.toc
*.out
# misc
todo.txt
.DS_Store
data.zip
.positai
# base R graphics artifact (created by plot() under Rscript with no device)
Rplots.pdf
# docs/ is built and deployed by GitHub Actions (publish.yml), not committed
docs/
# _freeze/ is kept for freeze: auto (do not ignore)
# working files (kept off GitHub)
BOOK-PLAYBOOK.md
NOTES-TO-BOOK-SPEC.md
CRC-POLISH.md
Steps2Publication.md
BACKLOG.md
